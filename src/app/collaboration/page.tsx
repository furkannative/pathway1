'use client';

import CollaborationDashboard from '@/components/CollaborationHeatmap/CollaborationDashboard';

export default function CollaborationPage() {
  return (
    <main className="py-8 bg-gray-50 min-h-screen">
      <CollaborationDashboard />
    </main>
  );
}
